# ── PlatformIO / VS Code (firmware project) ──────────────────────────────
.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Wi-Fi credentials — copy include/secrets.h.example to include/secrets.h
# (also ignored from the repo root; kept here so the firmware/ folder is
#  self-contained if copied out on its own)
include/secrets.h

# OS / editor cruft
.DS_Store
*.swp
