# ── PlatformIO / VS Code (firmware project) ──────────────────────────────
.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# The firmware no longer reads this file — Wi-Fi credentials live in NVS and are
# entered at /wifi. The rule stays so any leftover copy from before that change
# still cannot be committed.
include/secrets.h

# OS / editor cruft
.DS_Store
*.swp
